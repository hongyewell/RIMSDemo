<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.rims_new"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="14"
        android:targetSdkVersion="18" />
    
    <uses-permission android:name="android.permission.INTERNET"/>
    
    <application
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name" 
        android:theme="@style/AppTheme" >
        <activity
            android:name=".SplashActivity"          
            >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".mainActivity" android:label="@string/app_name"/>
        <activity android:name=".secondActivity" android:label="@string/app_name"/>
        <activity android:name=".PeopledetailActivity" android:label="@string/title_peopleDetail"/>
        <activity android:name=".Modifypeople" android:label="@string/title_modifyPeople"/>
        <activity android:name=".SysApplication" android:label="@string/app_name"/>
        <activity android:name=".People_houseInfo_Activity" android:label="@string/title_houseInfo"></activity>
    </application>

</manifest>
